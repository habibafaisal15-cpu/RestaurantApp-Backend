{
  "name": "restaurant-app-backend",
  "private": true,
  "version": "1.0.0",
  "description": "Vercel serverless wrapper for restaurant delivery API",
  "dependencies": {
    "serverless-http": "^3.2.0"
  },
  "scripts": {
    "postinstall": "npm install --prefix backend"
  }
}
