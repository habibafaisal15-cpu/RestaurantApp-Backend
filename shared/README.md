# Shared

API contracts, types, and constants consumed by the backend (and optionally published for the frontend team).

```
shared/
├── types/        # OrderStatus, PaymentMethod, Rider, Zone, MenuItem, …
└── constants/    # Status enums, WebSocket event names, API path constants
```
